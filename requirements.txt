# Core dependencies
# Install with: uv sync  (recommended, uses pyproject.toml)
# Or: uv pip install -r requirements.txt
typer>=0.12.0
rich>=13.7.0
psutil>=5.9.0
py-cpuinfo>=9.0.0
gputil>=1.4.0

# Development (install with: uv sync --extra dev)
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.3.0
mypy>=1.8.0

# PDF support (optional, install with: uv sync --extra pdf)
fpdf2>=2.7.0
